on:
  push:
    branches:
      - master
name: Push Event
jobs:
  build:
    name: Run build
    runs-on: ubuntu-latest
    outputs:
      preid: ${{ steps.setPreid.outputs.preid }}
    steps:
      - uses: actions/checkout@v1
        - name: Create value as an environment variable
        run: |
          echo -e "::set-output name=preid::$(date +'%s')"